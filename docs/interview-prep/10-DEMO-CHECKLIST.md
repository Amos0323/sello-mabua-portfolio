# Demo Checklist

## Practice First

Start with Employee Shuttle Tracker.

```powershell
cd C:\Users\SelloMa\Downloads\sello-portfolio\portfolio-project-audit\employee-shuttle-tracker
npm install
npm run dev
```

Open `http://localhost:3000` and `http://localhost:3000/driver`.

## Before Any Interview

- [ ] Open the portfolio site.
- [ ] Confirm job title says Systems Solution Specialist.
- [ ] Confirm no Download CV button appears.
- [ ] Keep one terminal per running app.
- [ ] Run tests before the call if time allows.
- [ ] Keep this docs folder open for demo notes.

## Project Commands

ShopSure:

```powershell
cd C:\Users\SelloMa\Downloads\sello-portfolio\portfolio-project-audit\ShopSure
npm run typecheck
npm test
```

Employee Shuttle Tracker:

```powershell
cd C:\Users\SelloMa\Downloads\sello-portfolio\portfolio-project-audit\employee-shuttle-tracker
npm run lint
npm run typecheck
npm test
npm run build
```

AI Document Intelligence Platform:

```powershell
cd C:\Users\SelloMa\Downloads\sello-portfolio\portfolio-project-audit\ai-document-intelligence-platform
npm run lint
npm run typecheck
npm test
npm run build
```

Bookly AI:

```powershell
cd C:\Users\SelloMa\Downloads\sello-portfolio\portfolio-project-audit\bookly-ai
npm test
npm run build
```

ReplyFlow AI:

```powershell
cd C:\Users\SelloMa\Downloads\sello-portfolio\portfolio-project-audit\replyflow-ai
npm test
npm run build
```

Remindly AI:

```powershell
cd C:\Users\SelloMa\Downloads\sello-portfolio\portfolio-project-audit\remindly-ai
npm test
npm run build
```

## Fast Demo Order

1. Employee Shuttle Tracker: employee view plus driver portal.
2. ShopSure: run Playwright tests and show HTML report.
3. AI Document Intelligence Platform: dashboard, review queue, extraction pipeline.
4. ReplyFlow AI: generate and approve lead reply.
5. Bookly AI: booking chat and available slot.
6. Remindly AI: reminder generation and schedule steps.

## Recovery Lines

- If an AI app says demo mode: "That is expected without an OpenAI key. The fallback path keeps the demo reliable."
- If a public demo site is slow: "The test depends on a public external demo site, so I would rerun or show the previous report."
- If asked about security warnings: "I found dependency audit warnings in the larger apps and recorded them as maintenance items before production use."

